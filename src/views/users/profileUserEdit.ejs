<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head',{title:"Editar usuario"})%>

<body>

    <%- include('../partials/header-admin')%>

    <main>
        <div class="products-edit-top">
            <h2>EDITAR Usuario : <%= user.email %></h2>
        </div>
        <div class="products-edit-main">
            <div class="title-line"></div>

            <form action="/users/edit?_method=PUT" method="POST" class="products-edit-form">   <!--enctype="multipart/form-data"-->

               <div class="form-element">
                    <label for="name">Nombre:</label>
                    <input type="text" name="name" id="apellido" value="<%= locals.oldData ? oldData.fullName : user.name %>">
                </div>

                <div class="form-element">
                    <label for="lastname">Apellido:</label>
                    <input type="text" name="lastname" id="lastname" value="<%= locals.oldData ? oldData.fullName : user.lastname %>">
                </div>

                <div class="form-element">
                    <label for="dni">DNI:</label>
                    <input type="text" name="dni" id="dni" value="<%= locals.oldData ? oldData.fullName : user.dni %>">
                </div>
                <div class="form-element">
                    <label for="address">Dirección:</label>
                    <input type="text" name="address" id="address" value="<%= locals.oldData ? oldData.fullName : user.address %>">
                </div>
                <div class="form-element">
                    <label for="phone">Teléfono:</label>
                    <input type="text" name="phone" id="phone" value="<%= locals.oldData ? oldData.fullName : user.phone %>">
                </div>

                <div class="form-element">
                    <label for="email">Email:</label>
                    <input type="email" name="email" id="email" value="<%= locals.oldData ? oldData.fullName : user.email %>">
                </div>
                <div class="form-element">
                    <label for="password">Contraseña:</label>
                    <input type="password" name="password" id="password" value="<%= locals.oldData ? oldData.fullName : user.password %>">
                </div>
                <div class="form-element">
                    <label for="image">Imagen:</label>
                    <input type="image" name="image" id="image" value="/img/users/<%= user.image %>">
                </div>
                    <div class="edit-buttons">
                        <a href="/users/profile/" class="form-button form-button-secondary">CANCELAR</a>
                        <button type="reset" class="form-button form-button-secondary">RESTABLECER</button>
                        <button type="submit"  class="form-button form-button-primary">GUARDAR</button>
                    </div>
            </form>
        </div>
    </main>


</body>

</html>


